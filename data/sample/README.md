# Sample data

Place small demo CSVs here for educational labs and CI without shipping full plant datasets.

Suggested files:

- `solar_sample.csv` — subset of `data/processed/solar_cleaned.csv`
- `weather_hourly_sample.json` — cached Open-Meteo / WeatherAPI response

The forecasting package looks for sample CSVs when processed files are missing.
