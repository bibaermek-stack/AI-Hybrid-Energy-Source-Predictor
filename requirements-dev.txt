# Development / CI only (not required for runtime Docker image)
ruff>=0.6.0
black>=24.0.0
# unittest is stdlib; pytest optional
pytest>=8.0.0
# Mobile smoke tests import the camera extension's API; must match flet's pin
# (mobile/pyproject.toml, checked by tests/test_mobile_smoke.py).
flet-camera==1.0.1
